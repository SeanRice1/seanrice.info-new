{
  "name": "seanrice.info",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.5.0",
    "react-dom": "^16.5.0",
    "react-scripts": "^3.3.0"
  },
  "scripts": {
    "css-build": "node-sass --omit-source-map-url ./src/styling/sass/bulma-styles.scss ./src/styling/css/bulma-styles.css",
    "css-watch": "npm run css-build -- --watch",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "devDependencies": {
    "bulma": "^0.8.0",
    "node-sass": "^4.9.3"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
